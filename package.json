{
  "name": "sontab",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "icons": "node scripts/build-icons.mjs",
    "test": "vitest run",
    "package": "rm -rf dist sontab-v$npm_package_version.zip && npm run build && cd dist && zip -rX ../sontab-v$npm_package_version.zip . -x 'node_modules/*' '.*' && cd .. && echo 'Created sontab-v'$npm_package_version'.zip'"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "@sveltejs/vite-plugin-svelte": "^7.2.0",
    "@tsconfig/svelte": "^5.0.8",
    "@types/chrome": "^0.2.2",
    "svelte": "^5.56.4",
    "svelte-check": "^4.7.2",
    "typescript": "^5.9.3",
    "vite": "^8.1.4",
    "vitest": "^4.1.10"
  }
}
